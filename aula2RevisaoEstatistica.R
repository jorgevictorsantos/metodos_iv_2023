# Revisão de estatística

# Medidas de tendência central

# Média
# média amostral

a <- c(1:10)
b <- c(rep(5, 5))
c <- c(1, 3, 5, 7, 9, 11)
d <- c(-5:5)

mean(a)
(sum(a)/length(a))

mean(b)
mean(c)
mean(d)


# variável aleatória

# variável aleatória discreta: somente valores finitos
# variável aleatória contínua: apresenta infinitos valores

# Esperança matemática: média de uma variável aleatória

# Variância na variável aleatória: desvios em torno da esperança



